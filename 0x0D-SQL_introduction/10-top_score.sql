-- list records of second_table in database hbtn_0c_0 including only name and score fields
-- starting from the records with highest score to lowest
SELECT score, name FROM second_table ORDER BY score DESC;
