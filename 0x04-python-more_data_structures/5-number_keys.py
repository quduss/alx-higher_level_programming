#!/usr/bin/python3
def number_keys(a_dictionary):
    """count number of keys in a_dictionary
    Args:
        a_dictionary: dictionary
    Returns:
        number of keys present
    """
    return len(list(a_dictionary))
